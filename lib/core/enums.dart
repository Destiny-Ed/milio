enum ProjectState { draft, invited, active, inProgress, review, completed }
enum InvitationState { pending, accepted, declined, expired }
enum MilestoneState { pending, inProgress, submitted, revisionRequested, approved, paid }
